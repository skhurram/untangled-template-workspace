package navis;

import org.junit.Test;

import static org.junit.Assert.assertEquals;

public class SampleTest {
    @Test
    public void does_stuff() {
        assertEquals(1, 2);

        /*
        IDMap seededDataIDs = injectDatabase(DBName,seed);

        doOperation(seededDataIDs.getID("userA"));

        releaseDatabase(DBName);
        */
    }
}
